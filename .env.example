# 📋 使用说明：
# 1. 将此文件复制为 .env 并填入你的实际配置值
# 2. 每行格式为 KEY=VALUE，不要加引号或空格
# 3. 修改后重启服务使配置生效
# 4. 根据你的需求选择对应的ASR方案，删除其他方案的配置

# OpenAI API密钥（可选，但推荐填写）
OPENAI_API_KEY=sk-your-openai-api-key-here

# 自定义LLM服务配置（可选）
# 如果使用第三方OpenAI兼容服务，请填写以下配置
LLM_API_KEY=your-llm-api-key-here
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# =============================================================================
#  方案1：OpenAI Whisper（推荐）

ASR_PROVIDER=openai
ASR_MODEL=whisper-1

# =============================================================================
# 方案2：国内ASR

ASR_PROVIDER=ASR
#访问密钥
ACCESS_KEY_ID=your-access-key-id
ACCESS_KEY_SECRET=your-access-key-secret
# 智能语音交互项目AppKey
NLS_APP_KEY=your-app-key

# =============================================================================
# =============================================================================
# 适合：隐私敏感、离线环境、无网络依赖
# 注意：需要较强的硬件配置，处理速度较慢

ASR_PROVIDER=faster_whisper
ASR_MODEL=small  # 可选：tiny, base, small, medium, large

# 本地模型目录（可选，避免重复下载）
# ASR_LOCAL_DIR=E:\models\faster-whisper-small

# =============================================================================
# ⚙️ 高级配置（可选）
# =============================================================================

# 音频分块处理（单位：分钟）
# 适合内存不足的环境，0为关闭（默认）
ASR_CHUNK_MIN=0

# 处理超时时间（秒）
# PROCESS_TIMEOUT=3600

# 日志级别
# LOG_LEVEL=INFO